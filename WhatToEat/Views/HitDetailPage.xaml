<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Recipes.Views.HitDetailPage" Title="{Binding Title}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add to My Recipes" Command="{Binding AddItemCommand}"/>
    </ContentPage.ToolbarItems>
    <StackLayout Spacing="15" Padding="20">        
        <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
            <Label Text="{Binding RecipeName}" FontSize="Large" FontAttributes="Bold" HorizontalTextAlignment="Center"/>
            <Image Source="{Binding ImageUrl}" />
        </StackLayout>
        <ScrollView>
            <StackLayout>
                <Label Text="Ingredients:" TextDecorations="Underline" FontSize="Medium"/>
                <Label Text="{Binding Ingredients}" LineBreakMode="WordWrap" Style="{DynamicResource ListItemTextStyle}" FontSize="Small" VerticalTextAlignment="Center"/>
                <Label Text="Recipe:" TextDecorations="Underline" FontSize="Medium"/>
                <Label FontSize="Small" FormattedText="{Binding RecipeBody}"/>
            </StackLayout>
        </ScrollView>
    </StackLayout>
</ContentPage>